@Library(['rigLibrary'])_
mavenCI{
scmUrl = 'https://github.com/digitalrigtest/XWQ23.git'
credentialsId = 'cred'
buildFileLocation = '<buildfile>'
buildCommands = '<buildCommands>'
sonarPropertiesFile = 'sonar-project.properties'
branchName = 'main'
//Docker build related variable. This can be removed if it is not a docker build.
tagName = 'dockerTag'
}
